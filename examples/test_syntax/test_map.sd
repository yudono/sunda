// Test Map Library Functions
import { obj_keys, obj_values, obj_entries, obj_has, obj_merge, obj_clone } from "map"

println("=== Map Library Tests ===")
println("")

// Test keys
println("1. Testing keys():")
var person = { name: "John", age: 30, city: "NYC" }
var keysList = obj_keys(person)
println("  keys({ name: 'John', age: 30, city: 'NYC' }):")
println("  " + keysList.map(k => k).length + " keys found")

// Test values
println("")
println("2. Testing values():")
var valuesList = obj_values(person)
println("  values({ name: 'John', age: 30, city: 'NYC' }):")
println("  " + valuesList.length + " values found")

// Test has
println("")
println("3. Testing has():")
var hasName = obj_has(person, "name")
var hasEmail = obj_has(person, "email")
println("  has(person, 'name') = " + hasName)
println("  has(person, 'email') = " + hasEmail)

// Test merge
println("")
println("4. Testing merge():")
var obj1 = { a: 1, b: 2 }
var obj2 = { b: 3, c: 4 }
var merged = obj_merge(obj1, obj2)
println("  merge({ a: 1, b: 2 }, { b: 3, c: 4 })")
println("  Merged object created (b should be 3)")

// Test clone
println("")
println("5. Testing clone():")
var original = { x: 10, y: 20 }
var cloned = obj_clone(original)
println("  clone({ x: 10, y: 20 })")
println("  Cloned object created")

println("")
println("=== All Map Tests Complete ===")
