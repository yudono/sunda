function fibonacci(n) {
    var a = 0
    var b = 1

    var i = 0
    while (i < n) {
        var tmp = a + b
        a = b
        b = tmp
        i = i + 1
    }
    return a
}

println(fibonacci(10))
